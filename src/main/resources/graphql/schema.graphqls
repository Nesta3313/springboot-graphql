type Query {
    car(id: ID!): Car
}

type Mutation {
    addCar(
        id: String!
        name: String!,
        color: String!,
        year: String!,
        location: String!): Car!
}

type Car {
    id: ID!
    name: String!
    color: String!
    year: Int!
    location: String!
}